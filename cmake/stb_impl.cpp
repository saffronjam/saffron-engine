// stb_image_write is header-only; exactly one translation unit must define
// STB_IMAGE_WRITE_IMPLEMENTATION. The renderer module includes the header
// (declarations only) in its global module fragment.
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include <stb_image_write.h>
